/*
 * detail.js
 *
 * 사원 상세 화면 전용 JavaScript
 *
 * 현재 detail.html은 단순 조회 화면이라 필수 JS 기능은 없다.
 *
 * 그래도 파일을 분리해두면 나중에 아래 기능을 추가하기 좋다.
 * - 삭제 버튼 confirm
 * - 이미지 클릭 시 확대 미리보기
 * - 상태 변경 이력 모달
 */
document.addEventListener("DOMContentLoaded", function () {
    // 사원 상세 화면이 로드되었을 때 실행된다.

    // 현재는 별도 동작 없음.
});
